# ربط التطوير المباشر بالسيرفر (CI/CD)

## الإعداد السريع

### 1. أضف أسرار GitHub (Secrets)
اذهب إلى: `Settings > Secrets and variables > Actions`

أضف هذه الأسرار:

| المتغير | القيمة |
|--------|--------|
| `VPS_HOST` | `147.93.120.99` |
| `VPS_USER` | `root` |
| `VPS_SSH_KEY` | محتوى المفتاح الخاص |

#### كيفية الحصول على VPS_SSH_KEY:
من جهازك المحلي (حيث لديك SSH مفتاح):
```bash
cat ~/.ssh/id_rsa  # أو اسم مفتاحك الخاص
# انسخ المحتوى بالكامل وأضفه كـ secret
```

### 2. كيفية الاستخدام

**بعد أي تعديل على الكود:**
```bash
git add .
git commit -m "وصف التعديل"
git push origin copilot/add-integration-repository-setup
```

**تلقائياً سيحدث:**
✅ بناء الواجهة (npm run build)
✅ رفع الملفات للسيرفر
✅ تحديث قاعدة البيانات
✅ إعادة تشغيل الخدمة

### 3. مراقبة النشر
اذهب إلى: `GitHub > Actions > Auto Deploy to onlainee.space`

ستشاهد حالة النشر مباشرة وأي أخطاء.

### 4. اختبار الـ Workflow يدويًا
```
Actions > Auto Deploy to onlainee.space > Run workflow > Branch: copilot/add-integration-repository-setup > Run
```

## خطوات النشر التي تحدث تلقائياً

1. **بناء الواجهة (Build):**
   - تثبيت npm ci
   - تشغيل npm run build
   - إنشاء مجلد dist

2. **الرفع للسيرفر (Deploy):**
   - الاتصال عبر SSH
   - سحب آخر تحديثات Git
   - تثبيت الحزم
   - إعداد قاعدة البيانات
   - بناء الواجهة
   - إعادة تشغيل الخدمة

3. **التحقق:**
   - عرض حالة الخدمة

## مثال: تحديث سريع

إذا عدلت ملف React مثلاً:

```bash
# 1. عدّل الملف
nano components/Dashboard.tsx

# 2. احفظ التغييرات
git add components/Dashboard.tsx
git commit -m "تحسين لوحة التحكم"
git push origin copilot/add-integration-repository-setup

# 3. اذهب إلى GitHub > Actions
# 4. شاهد حالة النشر
# 5. بعد الانتهاء: https://onlainee.space محدّث تلقائياً
```

## توقف النشر التلقائي مؤقتاً

إذا أردت تعديلات بدون نشر فوري، استخدم:
```bash
git push origin copilot/add-integration-repository-setup --no-workflow
```
أو غيّر الفرع مؤقتاً.

## حل مشاكل شائعة

**❌ "Authentication failed"**
- تحقق من VPS_SSH_KEY صحيح (بدون سطور إضافية)
- تأكد VPS_HOST و VPS_USER صحيحة

**❌ "Permission denied"**
- تأكد من أن المستخدم لديه صلاحيات sudo

**❌ "npm ci failed"**
- قد تحتاج لتحديث node_modules يدويًا على السيرفر

## التطوير المحلي

للعمل محلياً قبل النشر:
```bash
npm ci
npm run dev           # لتشغيل الواجهة
npm run dev:backend   # في terminal آخر للخلفية
```

اختبر تماماً ثم ادفع التغييرات.

---

**النتيجة:** تطوير وتحديث مستمر وسلس بدون تدخل يدوي! 🚀
